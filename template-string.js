const fname = 'Delwar';
const lname = 'Hossain';
const products = {shirtColor : 'Black', price : '$50', size: 'M', length : '38'};

const output = `My name is ${fname} ${lname}.I have a Shirt which color is ${products.shirtColor}. I have buy it ${products.price}`;
console.log(output);